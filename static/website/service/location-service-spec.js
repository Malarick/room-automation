describe('locationService', function() {

  beforeEach(module('CWI'));

  it('should ...', inject(function(locationService) {

    //expect(locationService.doSomething()).toEqual('something');

  }));

});
