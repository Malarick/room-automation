describe('authenticationService', function() {

  beforeEach(module('CWI'));

  it('should ...', inject(function(authenticationService) {

    //expect(authenticationService.doSomething()).toEqual('something');

  }));

});
