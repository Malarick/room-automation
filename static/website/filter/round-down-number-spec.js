describe('roundDownNumber', function() {

    beforeEach(module('CWI'));

    it('should ...', inject(function($filter) {

        var filter = $filter('roundDownNumber');

        expect(filter('input')).toEqual('output');

    }));

});
